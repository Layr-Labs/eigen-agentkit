{
  "name": "@eigenlayer/agentkit-opacity",
  "version": "0.1.0",
  "description": "Opacity adapter for verifiable inference in EigenLayer AgentKit",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "ai",
    "agents",
    "verifiable-inference",
    "eigenlayer",
    "opacity",
    "zktls"
  ],
  "author": "Eigen Labs, Inc.",
  "license": "MIT",
  "dependencies": {
    "@eigenlayer/agentkit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  },
  "publishConfig": {
    "access": "public"
  }
}