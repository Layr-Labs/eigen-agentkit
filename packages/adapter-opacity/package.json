{
  "name": "@layr-labs/agentkit-opacity",
  "version": "0.1.1",
  "description": "Opacity adapter for verifiable inference in EigenLayer AgentKit",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "jest --passWithNoTests"
  },
  "keywords": [
    "ai",
    "agents",
    "verifiable-inference",
    "eigenlayer",
    "opacity",
    "zktls"
  ],
  "author": "Eigen Labs, Inc.",
  "license": "MIT",
  "dependencies": {
    "@layr-labs/agentkit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.2.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.12",
    "ts-jest": "^29.1.2"
  },
  "publishConfig": {
    "access": "public"
  }
}